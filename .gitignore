# ── Python ─────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
.eggs/

# ── Virtual Environments ───────────────────────────────────────────────
.venv/
venv/
env/
ENV/

# ── Testing ────────────────────────────────────────────────────────────
.pytest_cache/
.coverage
htmlcov/
*.xml

# ── Jupyter / IPython ──────────────────────────────────────────────────
.ipynb_checkpoints/

# ── OS / Editor ────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/

# ── Large ML Artifacts (keep locally, NOT in git) ──────────────────────
# Only xgboost_pipeline.joblib and xgboost_preprocessor.joblib are needed at runtime.
# Regenerate others via train_xgboost.py / create_final_model.py
artifacts/final_pipeline.joblib
artifacts/feature_info.joblib
artifacts/xgboost_feature_info.joblib
artifacts/xgboost_model.json

# ── Large Synthetic Datasets (too big for git) ─────────────────────────
# Regenerate via generate_dataset.py then ingest.py
synthetic_drug_conversations.csv
synthetic_emoji_dataset.csv
synthetic_slang_dataset.csv
synthetic_emoji_sample.csv
synthetic_slang_sample.csv

# ── ChromaDB Vector Store (generated by ingest.py) ─────────────────────
chroma_data/

# ── Runtime / Temp files ───────────────────────────────────────────────
ingest_checkpoint.json
*.log

# ── Load Test Reports (generated by locust) ────────────────────────────
load_test_report*.html

# ── Documentation Source (keep PDF, drop Word source) ──────────────────
DATASET_DOCUMENTATION.docx
